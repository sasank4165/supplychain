# Example Environment Variables for Supply Chain Agentic AI Application
# Copy this file to .env and update with your actual values

# AWS Configuration
AWS_REGION=us-east-1
ENVIRONMENT=dev
SC_AGENT_PREFIX=sc-agent

# Athena Configuration
ATHENA_DATABASE=supply_chain_db
ATHENA_CATALOG=AwsDataCatalog
ATHENA_OUTPUT_LOCATION=s3://your-athena-results-bucket/

# DynamoDB Tables
DYNAMODB_SESSION_TABLE=sc-agent-sessions
DYNAMODB_MEMORY_TABLE=sc-agent-memory
DYNAMODB_CONVERSATION_TABLE=sc-agent-conversation

# Lambda Functions
LAMBDA_SQL_EXECUTOR=sc-agent-sql-executor
LAMBDA_INVENTORY_OPTIMIZER=sc-agent-inventory-optimizer
LAMBDA_LOGISTICS_OPTIMIZER=sc-agent-logistics-optimizer
LAMBDA_SUPPLIER_ANALYZER=sc-agent-supplier-analyzer

# Bedrock Configuration
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# Cognito Configuration
USER_POOL_ID=us-east-1_XXXXXXXXX
USER_POOL_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# Secrets (for local development only - use AWS Secrets Manager in production)
# SECRET_DATABASE_CONNECTION_STRING=postgresql://user:password@host:5432/database
# SECRET_API_EXTERNAL_API_KEY=your-api-key-here

# Parameters (for local development only - use AWS Parameter Store in production)
# PARAM_ATHENA_DATABASE=supply_chain_db
# PARAM_ATHENA_OUTPUT_LOCATION=s3://your-bucket/
